class Solution {
  public:
    long long sumOfSeries(long long N) {
        long long int sum=0;
        for (long long int i=1;i<=N;i++){
            sum=sum+(i*i*i);
            
            
        }
        return sum;
    }
};
